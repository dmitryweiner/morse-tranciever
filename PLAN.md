# План улучшений

Согласовано 2026-07-19. ВСЕ 5 ЭТАПОВ ВЫПОЛНЕНЫ 2026-07-19 (итоги — в
заголовках этапов). Порядок — от быстрых побед к крупной работе; каждый
этап самостоятелен, завершается зелёным `npm run check` и регрессией на
`samples/`. Процесс — TDD: сначала тест, потом код. Без коммитов без просьбы.

Отложено (бэклог, сознательно не взяли сейчас): словарная/контекстная
коррекция декодера, тренировочные режимы, RX-диагностика в UI, гашение цикла
на скрытой вкладке.

---

## 1. Адаптивное отношение тире/точки в декодере — СДЕЛАНО 2026-07-19

Итог: кластеризация по средним + оценка ratio (EMA, [1.5,4]) + порог
unit×√ratio в неоднозначном окне. Новые тесты: «PARIS PARIS» и
«PARIS OOO EEE» на 1.9:1, сброс ratio при setUnitMs. Все 7 samples — без
изменений результатов; TEST.wav остался «TENT»: слившаяся пара точек
(~495 мс) по длительности РАВНА тире этой пищалки — по таймингам
неразличимо, нужен словарь (бэклог). Причина зафиксирована в CLAUDE.md.

**Проблема.** `classifyAndAdapt` жёстко предполагает тире = 3×точки: порог
классификации `2×unit`, неоднозначное окно сравнивает `dur` против `dur/3`,
а порог кластеризации — геом. среднее min/max окна (хрупко к выбросам).
Реальная пищалка `samples/TEST.wav` держит тире ≈1.9×точки → «TENT».

**Что делаем** (`src/morse/decoder.ts`):
- Разделять окно меток на кластеры и брать **кластерные средние** (а не
  min/max); порог = геом. среднее средних кластеров.
- Вести оценку `ratio = avgDash / avgDot` (EMA, кламп ~[1.5, 4], дефолт 3);
  обновлять только из окон, где кластера два и в каждом ≥2 метки.
- Классификация в неоднозначном окне: кандидаты `dur` и `dur/ratio`;
  порог точка/тире — `unit × √ratio` вместо жёстких `2×unit`.
- `setUnitMs` (re-seed слайдером) сбрасывает и ratio к 3.

**Тесты:**
- Существующие decoder-тесты остаются зелёными (стандартный 3:1 — поведение
  не меняется).
- Новый: синтетическая «пищалка 1.9:1» декодируется верно после адаптации.
- `tests/samples.test.ts`: закреплённые результаты не хуже; цель —
  `TEST.wav` → «TEST» (если не выйдет — зафиксировать фактический прогресс
  и причину в CLAUDE.md).

**Риски.** Дрейф ratio на шумных окнах — поэтому обновление только из
чистых двухкластерных окон; junk/осколки в ratio не попадают (существующие
guard'ы остаются как есть).

---

## 2. Передача по тексту (TX) — СДЕЛАНО 2026-07-19

Итог: src/morse/sender.ts (TextSender поверх Keyer), 9 тестов (тайминги,
события, стоп, прогресс, неизвестные символы); UI: #sendInput + Send/Stop,
подсветка текущей буквы в #sendLine, обрыв по нажатию любого ключа/смене
режима; смоук в scripts/shot.mjs (desktop и mobile — зелёные).

**Что делаем.** Ввод строки → приложение отстукивает её с точными
PARIS-таймингами и боковым тоном. Заодно получаем генератор посылок для
отладки этапа 4.

- `src/morse/sender.ts` — чистый автомат `TextSender` по образцу
  `PaddleKeyer`: внутри обычный `Keyer` с синтетическими `keyDown/keyUp`
  (события — общий `MorseEvent`, дерево/текст рендерятся тем же
  `applyEvents`). API: `start(text, t)`, `stop()`, `tick(t) → MorseEvent[]`,
  флаг `isToneOn`, прогресс (индекс текущего символа) для UI.
- Паузы: элемент 1u, буква 3u, слово 7u; неизвестные символы
  пропускаются как пробел.
- UI (`src/main.ts` + `index.html`): в TX-панели поле ввода + кнопка
  Send/Stop; во время передачи подсвечивать текущую букву строки; ключи
  на время передачи не блокировать мозгами — просто Stop по любому нажатию
  ключа. Тон — тем же `Sidetone` (ensure() синхронно в жесте).
- UI-тексты — английские.

**Тесты:** длительности элементов/пауз на заданном WPM (скриптовое время,
как в тестах paddle); события идентичны ручному набору той же строки.
Смоук в `scripts/shot.mjs` — передача короткой строки кнопкой.

---

## 3. Цифры и знаки (без изменения дерева) — СДЕЛАНО 2026-07-19

Итог: таблица +0–9 и .,?/=+-@; TREE_DEPTH=4 отделён от MAX_CODE_LENGTH=6;
дерево обрезает путь (flash глубже дерева — no-op); тесты: ITU-споты,
round-trip, отправка «CQ DE 73 = OK?»; смоук shot.mjs передаёт «5.» и
проверяет живой DOM.

**Решение пользователя:** дерево остаётся A–Z (4 уровня); длинные коды
подсвечивают путь до 4-го уровня, дальше — только в строке кода.

- `src/morse/code.ts`: цифры 0–9 (длина 5) и базовые знаки ITU
  (`. , ? / = + - @`, длина 5–6). `MAX_CODE_LENGTH` больше не равен глубине
  дерева — разнести на `TREE_DEPTH = 4` (для `src/ui/tree.ts`) и фактическую
  максимальную длину кода.
- `src/ui/tree.ts`: `setPath`/`flash` не падают и корректно обрезают путь
  на глубине дерева.
- Декодер/кеер работают без изменений (им всё равно, что в таблице);
  `TextSender` из этапа 2 сразу умеет цифры.
- Клавиатурный ввод и глифы `#codeNow` — без изменений.

**Тесты:** таблица против ITU-R M.1677 (спот-проверки), round-trip
encode↔decode, `setPath` с кодом длины 6.

---

## 4. RX-фронтенд: AudioWorklet + Гёрцель — СДЕЛАНО 2026-07-19

Итог (с двумя сознательными отличиями от плана ниже):
- Гребёнка считается НЕ в воркете, а на main-потоке общим модулем
  src/analysis/spectrum.ts (SpectrumAnalyser) — стенд и браузер считают
  буквально одним кодом; воркет — тривиальный инлайн-форвардер PCM (Blob
  URL, без сборочных плясок), тайминги кадров всё равно сэмпловые. Нагрузка
  ~66 частот × 200 кадр/с — незначительна и для телефона; сырой PCM в main
  заодно готов для этапа 5 (запись).
- Кроме RELOCK/TTL/LOCK_MIN в мс пришлось привести к hop'у и кадровые EMA
  гейта (1-(1-α)^(hop/15)): иначе пол утраивал скорость подъёма и гасил
  тире 3 WPM (тест «does not starve»).
Анти-дребезг декодера: склейка <25 мс разрешена только к метке ≥0.45 юнита —
на хопе 5 мс «возня» перед посылкой рассыпается в град осколков 5–45 мс,
которые прежнее правило сшивало в ложные элементы (TEST.wav давал AENT,
TEST2 — EEAEST). После правки ВСЕ 7 samples — прежние закреплённые
результаты. Браузер: rx.mjs зелёный на 15 и 30 WPM (раньше 30 WPM на
AnalyserNode+15 мс не декодировался); rx --file TEST1 стал «T TENT» против
«H E T O T» на старом коде (фейковый захват качает уровень — эталон
по-прежнему офлайн-стенд).

**Проблема.** AnalyserNode: окно 21 мс + хоп 15 мс размазывают фронты и
квантуют длительности меток крупно; на быстрой морзянке съедаются паузы.
Офлайн-стенд уже считает Гёрцеля — браузер должен считать так же.

**Что делаем.**
- AudioWorklet-процессор (`src/audio/rx-worklet.ts`): копит PCM, каждые
  **5 мс** считает гребёнку Гёрцеля по полосе 300–3400 Гц с шагом ~47 Гц
  (окно ~21 мс, Блэкман — как `wavlab.goertzelPowerDb`), постит
  `SpectralFrame` + точный таймстемп (`currentFrame/sampleRate`) в main.
  Нагрузка ~66 частот × 200 кадров/с — ок, но замерить на мобиле.
- `src/audio/mic.ts`: push-модель вместо `poll()` — колбэк на кадр;
  getUserMedia-констрейнты и синхронное создание контекста не трогаем.
- `src/morse/envelope.ts`: константы в кадрах (`RELOCK_FRAMES`,
  `LOCK_TTL_FRAMES`, `LOCK_MIN_FRAMES`) перевести в **миллисекунды**,
  `SignalGate` принимает hop параметром — иначе смена хопа 15→5 мс
  втихую утроит все выдержки.
- `src/main.ts`: RX-ветка обрабатывает кадры по приходу (со временем кадра,
  не `performance.now`), общий 15-мс цикл остаётся для TX и UI.
- Офлайн-стенд: `runRxChain`/`spectralFrames` на hop 5 мс по умолчанию —
  стенд и браузер снова считают одинаково; пере-закрепить
  `tests/samples.test.ts` на новом хопе (ожидаемо не хуже; слипшиеся точки
  могут начать разделяться — обновить «известные пределы»).
- Пройтись по анти-дребезгу декодера: `GLITCH_MERGE_MS`/`MIN_MARK_UNITS`
  абсолютные или в юнитах — не зависят от хопа, но короткие разрывы,
  которые раньше не были видны на 15 мс, теперь появятся — проверить на
  всех `samples/`.

**Проверка:** unit-тесты gate с параметрическим хопом → `npm run wav` на
всех samples → `npm run rx` (проводка в браузере) → `npm run shot`.

**Риски.** CPU на слабых телефонах (замерить, при нужде проредить гребёнку
вне захвата несущей); поведенческие сдвиги гейта на новом хопе — ловим
регрессией samples до правки констант.

---

## 5. Кнопка записи звука в файл — СДЕЛАНО 2026-07-19 (переделано)

Уточнение пользователя: записывать нужно СВОЮ ПЕРЕДАЧУ, а не приём (первая
версия писала микрофон в RX — удалена). Итог: src/audio/txrecord.ts —
TxRecorder собирает фронты бокового тона (ключ/paddle/Send через
sidetoneOn/Off в main), renderToneWav синтезирует синус 16 кГц с рампами;
7 тестов, включая round-trip: записанное «SOS» декодируется реальной
RX-цепочкой стенда. Кнопка Record в TX-панели, таймер, потолок 120 с,
morse-tx-<дата>.wav. Попутно окно спектра стало масштабироваться к времени
(windowSizeFor: ~21 мс при любой частоте дискретизации — чинит и разбор
чужих записей 16–44.1 кГц, где окно растягивалось). Смоук в shot.mjs:
Record → Send E → download.

**Что делаем.** В RX-панели кнопка Record: во время прослушивания писать
сырой звук микрофона и по остановке отдавать WAV-файл (16-bit PCM —
формат, который уже понимают `npm run wav` и офлайн-стенд). Двойная
польза: пользователи получают записи своих сеансов, а проект — новые
реальные сэмплы для `samples/` и отладки декодера.

- Захват PCM из того же графа, что и анализ (после этапа 4 — тот же
  AudioWorklet копит сырые сэмплы; до него — отдельный ScriptProcessor/
  worklet рядом с AnalyserNode).
- Кодирование WAV — генератор заголовка уже есть в `src/audio/wav.ts`
  (проверить пригодность), скачивание — Blob + временная ссылка.
- Ограничить длительность записи (например, ~2 мин), чтобы не съесть
  память телефона; показывать таймер записи.
- UI-тексты — английские.

**Тесты:** кодирование WAV — чистый юнит-тест (round-trip через
`decodeWavPcm16` из wavlab); проводка — вручную/скриншот-смоук.

---

## Порядок и зависимости

1 (декодер) → 2 (TX по тексту) → 3 (цифры/знаки) → 4 (worklet) →
5 (запись в файл). Этап 2 даёт генератор посылок, полезный при отладке
этапа 4; этап 3 тривиально ложится после 2; этап 5 удобнее делать после 4
(worklet уже держит сырой PCM), но при нужде можно и раньше — отдельным
узлом захвата. После каждого этапа — обновить CLAUDE.md (карта модулей/
бэклог) и README.
